# =============================================================================
# Deutsch — Öffentliche Lauf- und Build-Abhängigkeiten
# Pillow: UI (abgerundete Buttons, ggf. Icons)
# PyInstaller: EXE bauen (siehe builder.py / UgreenNASAdmin.spec)
# =============================================================================
# English — Public run and build dependencies
# Pillow: UI (rounded buttons, icons as needed)
# PyInstaller: build EXE (see builder.py / UgreenNASAdmin.spec)
# =============================================================================

Pillow>=9.0
pyinstaller>=6.0

# Optional: Passwort im OS-Tresor (🔐 PW Tresor) — pip install keyring
# Optional: store password in OS vault — pip install keyring
