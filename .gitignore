# Secrets & local config (never commit)
nas_admin_connection.json
telegram_notify.json
.env.web

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/

# PyInstaller / builds
build/
dist/
*.spec.bak

# Öffentlicher Unterordner: eigenes Git-Repo (siehe README → GitHub)
# Inhalt von öffentlich/ nicht im privaten Haupt-Repo versionieren.
öffentlich/

# Lokale Binaries (nicht ins private Repo)
nastools/*.exe

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
